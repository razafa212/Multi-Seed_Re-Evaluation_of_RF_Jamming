# ─────────────────────────────────────────────────────────────
# requirements.txt
# Protocol-Controlled Multi-Seed Re-Evaluation of RF Jamming
# Intrusion Detection in Vehicular Networks
#
# Tested environment: Google Colab (Python 3.10, CUDA 12.x)
# ─────────────────────────────────────────────────────────────

# --- NLP / BERT (version-pinned from notebooks) ---
transformers==4.44.2
tokenizers==0.19.1
huggingface-hub==0.23.4

# --- Deep learning ---
# PyTorch: used for BERT fine-tuning (AdamW, DataLoader, cuda sync)
torch>=2.0.0

# TensorFlow/Keras: used for BiLSTM (Bidirectional, LSTM, EarlyStopping, etc.)
tensorflow>=2.13.0

# --- Gradient boosting (meta-learner) ---
xgboost>=1.7.0

# --- Classical ML & statistics ---
scikit-learn>=1.3.0
scipy>=1.11.0

# --- Data handling ---
numpy>=1.24.0
pandas>=2.0.0

# --- Visualisation ---
matplotlib>=3.7.0
seaborn>=0.12.0

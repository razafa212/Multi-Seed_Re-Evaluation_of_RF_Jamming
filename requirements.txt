# Requirements for: Protocol-Controlled Multi-Seed Re-Evaluation
#                    of RF Jamming IDS in Vehicular Networks
#
# Tested on: Google Colab (T4 GPU), Python 3.10
# TensorFlow and PyTorch versions match the benchmark JSON outputs.
# ──────────────────────────────────────────────────────────────────

# Core ML
tensorflow==2.20.0
torch==2.11.0
xgboost>=2.0,<3.0
scikit-learn>=1.3,<2.0

# Data
numpy>=1.24,<2.0
pandas>=2.0,<3.0

# Statistical tests (T1, T2, Extra notebooks)
scipy>=1.11,<2.0

# BERT WordPiece baseline
tokenizers==0.19.1
transformers==4.44.2
huggingface-hub==0.23.4

# Utilities
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.60

# CUDA (for GPU reproducibility)
# cuDNN and CUDA are provided by the Colab runtime.
# If running locally, install:
#   CUDA 12.8  (matches torch 2.11.0+cu128)
#   cuDNN 9.x  (compatible with TF 2.20.0)
